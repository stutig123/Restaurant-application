---
- name: Install dependencies on the server
  hosts: web
  become: yes
  tasks:
    - name: Update apt
      apt:
        update_cache: yes
    - name: Install Nginx
      apt:
        name: nginx
        state: present
